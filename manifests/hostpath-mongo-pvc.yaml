apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hostpath-mongo-pvc 
spec:
  storageClassName: localstorage
  resources:
    requests:
      storage: 1Gi
  accessModes:
    - ReadWriteOnce